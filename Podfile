# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'photoGalleryTask' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'
  pod "KRProgressHUD"
  pod 'Kingfisher', '~> 6.0'

  # Pods for photoGalleryTask

  target 'photoGalleryTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'photoGalleryTaskUITests' do
    # Pods for testing
  end

end
